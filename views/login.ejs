<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Login</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-zinc-950 text-white min-h-screen flex items-center justify-center">

  <!-- Background Glow -->
  <div class="absolute inset-0">
    <div class="absolute top-20 left-20 w-72 h-72 bg-blue-600/30 blur-3xl rounded-full"></div>
    <div class="absolute bottom-20 right-20 w-72 h-72 bg-purple-600/30 blur-3xl rounded-full"></div>
  </div>

  <!-- Login Card -->
  <div class="relative w-full max-w-md p-8 rounded-2xl bg-zinc-900/70 backdrop-blur-xl border border-zinc-800 shadow-2xl">

    <!-- Heading -->
    <h1 class="text-3xl font-bold tracking-tight">Welcome Back 👋</h1>
    <p class="text-zinc-400 mt-2 text-sm">
      Login to continue to your dashboard.
    </p>

    <!-- Form -->
    <form action="/login" method="post" class="mt-8 space-y-5">

      <!-- Email -->
      <div>
        <label class="text-sm text-zinc-300">Email</label>
        <input 
          name="email" 
          placeholder="Enter your email"
          class="mt-2 w-full px-4 py-3 rounded-xl bg-zinc-800 text-white placeholder-zinc-500 
                 border border-zinc-700 focus:outline-none focus:ring-2 focus:ring-blue-600 focus:border-blue-600 transition"
        />
      </div>

      <!-- Password -->
      <div>
        <label class="text-sm text-zinc-300">Password</label>
        <input 
          name="password" 
          placeholder="Enter your password"
          type="password"
          class="mt-2 w-full px-4 py-3 rounded-xl bg-zinc-800 text-white placeholder-zinc-500 
                 border border-zinc-700 focus:outline-none focus:ring-2 focus:ring-blue-600 focus:border-blue-600 transition"
        />
      </div>

      <!-- Button -->
      <input 
        type="submit" 
        value="Login"
        class="w-full py-3 rounded-xl bg-blue-600 hover:bg-blue-500 transition font-semibold shadow-lg shadow-blue-600/30 cursor-pointer"
      />

    </form>

    <!-- Footer -->
    <p class="text-center text-sm text-zinc-400 mt-6">
      Don’t have an account?
      <a href="/signup" class="text-blue-500 hover:text-blue-400 font-medium">Sign up</a>
    </p>

  </div>

</body>
</html>























<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>login</title>
    <script src = "https://cdn.tailwindcss.com"></script>
</head>
<body>
    
    <div class="main bg-zinc-900 min-h-screen w-full">
        <div class="form p-64">
            <form action="/login" method = "post">
                 
                <input name = "email" placeholder = "Email" class = "bg-zinc-700 rounded-md w-full mt-4 h-7">
                <input name = "password" placeholder = "Password" type = "password" class = "bg-zinc-700 rounded-md w-full mt-4 h-7">
                <input type="submit" value="submit" class="bg-blue-600 text-white rounded-md mt-5 h-7 w-28">
            </form>
        </div>

    </div>
</body>
</html> -->



