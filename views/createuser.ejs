<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CreateUser</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="main bg-gray-300 min-h-screen w-full">
        <form action="/create" method = "post">

            <input name = 'Name' placeholder = "Name" class="bg-gray-600 rounded-md">
            <input 
                type="text"
                name="phone_number"
                placeholder="Enter Mobile Number"
                maxlength="10"
                inputmode="numeric"
                pattern="[0-9]{10}"
                required
                oninput="this.value = this.value.replace(/[^0-9]/g, '')"
                class="bg-gray-600 rounded-md">

            <input name = 'Email' placeholder = "Email" class="bg-gray-600 rounded-md" required>
            <input name = 'Password' placeholder = "Password" class="bg-gray-600 rounded-md" required>
            <input placeholder = "Create" type="submit" class="bg-blue-600 text-white rounded-md p-2">
        </form>


    </div>
    
</body>
</html>