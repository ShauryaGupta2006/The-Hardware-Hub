<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Products</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-zinc-950 text-white min-h-screen">

  <!-- Background Glow -->
  <div class="absolute inset-0 -z-10">
    <div class="absolute top-10 left-10 w-80 h-80 bg-blue-600/20 blur-3xl rounded-full"></div>
    <div class="absolute bottom-10 right-10 w-80 h-80 bg-purple-600/20 blur-3xl rounded-full"></div>
  </div>

  <!-- Navbar -->
  <nav class="w-full px-8 py-5 flex items-center justify-between border-b border-zinc-800 bg-zinc-950/60 backdrop-blur-xl sticky top-0 z-50">

    <div class="flex items-center gap-3">
      <div class="w-10 h-10 rounded-xl bg-blue-600 flex items-center justify-center font-bold text-lg shadow-lg shadow-blue-600/30">
        H
      </div>
      <h1 class="text-xl font-bold tracking-tight">The Hardware Hub</h1>
    </div>

    <div class="hidden md:flex items-center gap-8 text-sm text-zinc-300">
      <a href="/" class="hover:text-white transition">Home</a>
      <a href="/products" class="text-blue-400 font-medium">Products</a>
      <a href="/admin/dashboard" class="hover:text-white transition">Admin</a>
      <a href="/contact" class="hover:text-white transition">Contact</a>
    </div>

    <div class="flex items-center gap-3">
      <a href="/admin/listproduct"
        class="px-4 py-2 rounded-xl bg-blue-600 hover:bg-blue-500 transition text-sm font-semibold shadow-lg shadow-blue-600/30">
        ➕ Add Product
      </a>
    </div>
  </nav>

  <!-- Page Content -->
  <main class="max-w-6xl mx-auto px-8 py-12">

    <!-- Heading -->
    <div class="flex flex-col md:flex-row md:items-end md:justify-between gap-6">
      <div>
        <h2 class="text-4xl font-bold tracking-tight">All Products 🛒</h2>
        <p class="text-zinc-400 mt-2 text-sm">
          Browse all available hardware products in your store.
        </p>
      </div>

      <!-- Search -->
      <div class="w-full md:w-[420px]">
        <input
          placeholder="Search drill, bolts, hammer..."
          class="w-full px-4 py-3 rounded-xl bg-zinc-900/60 border border-zinc-800 
                 placeholder-zinc-500 focus:outline-none focus:ring-2 focus:ring-blue-600 transition"
        />
      </div>
    </div>

    <!-- Filters -->
    <div class="mt-8 flex flex-col sm:flex-row gap-4">

      <select
        class="px-4 py-3 rounded-xl bg-zinc-900/60 border border-zinc-800 text-zinc-200 
               focus:outline-none focus:ring-2 focus:ring-blue-600 transition"
      >
        <option>All Categories</option>
        <option>Drill Machines</option>
        <option>Bolts & Nuts</option>
        <option>Tool Kits</option>
        <option>Safety Gear</option>
        <option>Electrical</option>
      </select>

      <select
        class="px-4 py-3 rounded-xl bg-zinc-900/60 border border-zinc-800 text-zinc-200 
               focus:outline-none focus:ring-2 focus:ring-blue-600 transition"
      >
        <option>Sort By: Default</option>
        <option>Price: Low to High</option>
        <option>Price: High to Low</option>
        <option>Newest First</option>
        <option>Best Selling</option>
      </select>

      <button
        class="px-5 py-3 rounded-xl bg-zinc-900/60 border border-zinc-800 hover:border-blue-600 transition font-medium"
      >
        Reset Filters
      </button>

    </div>

    <!-- ✅ Products Grid (ONLY ONE GRID) -->
    <section class="mt-10 grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">

      <% products.forEach(function(product){ %>

        <!-- Product Card -->
        <div class="p-6 rounded-2xl bg-zinc-900/60 border border-zinc-800 hover:border-blue-600 transition shadow-xl">

          <!-- Image -->
          <div class="h-44 rounded-xl bg-zinc-950 border border-zinc-800 overflow-hidden flex items-center justify-center">
            <img class="w-full h-full object-cover" src="<%= product.image %>" alt="<%= product.name %>">
          </div>

          <!-- Title + Category -->
          <div class="mt-5 flex items-center justify-between">
            <h3 class="font-semibold text-lg"><%= product.name %></h3>

            <span class="text-xs px-3 py-1 rounded-full bg-blue-600/15 border border-blue-600/30 text-blue-400">
              <%= product.category || "General" %>
            </span>
          </div>

          <!-- Description -->
          <p class="text-zinc-400 text-sm mt-2">
            <%= product.description %>
          </p>

          <!-- Price + Button -->
          <div class="flex items-center justify-between mt-5">
            <p class="text-xl font-bold">₹<%= product.price %></p>

            <!-- View Product -->
            <a href="/products/<%= product._id %>"
              class="px-4 py-2 rounded-xl bg-blue-600 hover:bg-blue-500 transition text-sm font-semibold">
              View
            </a>
          </div>

        </div>

      <% }) %>

    </section>

    <!-- Footer -->
    <footer class="mt-16 border-t border-zinc-800 pt-10 text-center text-zinc-400 text-sm">
      © 2026 The Hardware Hub. Built for builders 🔧
    </footer>

  </main>

</body>
</html>
